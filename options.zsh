setopt auto_cd             # Automatically change to a directory if the command is a directory name.
setopt extended_history    # Append history lines as they’re executed.
setopt hist_ignore_dups    # Don’t record duplicate commands.
setopt share_history       # Share history among all sessions.

